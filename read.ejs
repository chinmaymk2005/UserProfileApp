<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title> 
    <link rel="stylesheet" href="/stylesheets/style.css"> 
    <script src="https://cdn.tailwindcss.com"></script> 
</head>
<body>
    
    <div class="w-full  bg-zinc-900 p-10 text-white">  

        <a class="text-blue-400 text-0.5xl" href="/">Back to  home</a>
        <h1 class="text-3xl tracking-tighter mb-3 mt-7" >All Users</h1> 
        <div class="user flex flex-wrap gap-5">  
            <% if(alluser.length > 0 ) { %> 
                <% alluser.forEach(function(user){ %>  

                    <div class="user w-72 h-full p-4 bg-zinc-800 rounded-lg" >  
                        <div class="w-full h-72 bg-zinc-500 rounded-lg overflow-hidden">
                            <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="">
                        </div>  
                        <h3 class="text-xl tracking-tighter mt-3" ><%= user.name  %></h3> 
                        <h5 class="text-zinc-500" ><%= user.email  %></h5>  
                        <div class="flex gap-3 mt-3">
                            <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit User</a> 
                            <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>  
                        </div>
                    </div>   
                <% }) %> 
            <% } else { %> 
                <h3 class="text-zinc-400 text-xl" >No user found !! </h3>
            <% } %>
                  

        </div>
    </div>  


</body>
</html> 